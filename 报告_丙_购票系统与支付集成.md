# 项目1：中超联赛赛事管理系统

## 1 项目说明

### 项目功能

本项目是一个中超联赛赛事管理系统，实现了以下核心功能：
- **球队管理**：管理中超16支球队的基本信息（名称、城市、主场、队徽等）
- **球员管理**：管理各球队球员信息及统计数据（进球、助攻、红黄牌、身价等）
- **新闻展示**：发布和展示中超联赛相关新闻资讯
- **联赛积分榜**：展示各赛季球队积分排名与多维统计数据
- **赛程查询**：查询历年中超联赛赛程安排
- **在线购票**：用户登录后可选座购票，集成支付宝沙箱支付

### 所用技术

| 类别 | 技术 |
|------|------|
| 后端框架 | Python 3 + Django 6.0.2 |
| 数据库 | SQLite3 |
| 前端 | HTML5 + CSS3 + JavaScript + Bootstrap |
| 支付集成 | 支付宝沙箱环境（python-alipay-sdk） |
| 版本控制 | Git |

### 运行平台

- 操作系统：macOS / Windows / Linux
- Python版本：3.10+
- 浏览器访问：http://127.0.0.1:8000/

---

## 2 自己工作说明

### 2.1 工作任务概述

我负责系统的**用户购票模块**和**支付宝支付集成**的开发。这是系统中业务逻辑最复杂的部分，涉及用户认证、购票流程、观赛人管理、交互式选座、以及支付宝沙箱支付对接。具体工作包括：

1. 设计并实现自定义 `EmailAuthBackend` 认证后端，支持用户使用邮箱+密码登录
2. 设计并实现 `TicketOrder`（购票订单）和 `Viewer`（观赛人）两个数据模型
3. 开发完整的购票流程（共8个视图函数）：登录 → 选球队 → 管理观赛人 → 选座 → 支付
4. 实现 AJAX 异步观赛人管理（添加/编辑/删除），无需刷新页面
5. 设计 SVG 交互式球场座位图，分4个区域对应不同票价（80-380元）
6. 集成支付宝沙箱 SDK，实现订单创建、支付链接生成、return 同步回调、notify 异步通知
7. 开发支付成功确认页面和订单状态管理
8. 配置购票相关的 Django Admin 后台管理

---

### 2.2 数据库表结构

#### （1）core_ticketorder 表（购票订单表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 主键，自增 |
| user_id | INTEGER | 外键，关联 auth_user（购票用户） |
| match_id | INTEGER | 外键，关联 matches_match（比赛） |
| ticket_price | INTEGER | 票档价格（元） |
| ticket_count | INTEGER | 购票张数（默认1） |
| id_card | VARCHAR(18) | 观赛人身份证号 |
| phone | VARCHAR(11) | 观赛人手机号 |
| total_amount | DECIMAL(10,2) | 订单总金额 |
| order_number | VARCHAR(32) | 订单号（唯一，格式：T+时间戳+随机码） |
| status | VARCHAR(20) | 订单状态（pending/paid/cancelled） |
| payment_qr_code | TEXT | 支付二维码数据 |
| created_at | DATETIME | 订单创建时间 |
| paid_at | DATETIME | 支付时间（可空） |

**约束**：`order_number` 唯一索引；默认按 `-created_at` 降序排列。

#### （2）core_viewer 表（观赛人表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 主键，自增 |
| user_id | INTEGER | 外键，关联 auth_user |
| name | VARCHAR(50) | 观赛人姓名 |
| id_card | VARCHAR(18) | 身份证号 |
| phone | VARCHAR(11) | 手机号 |
| created_at | DATETIME | 创建时间 |

**约束**：`(user_id, id_card)` 联合唯一，同一用户不能添加重复身份证号的观赛人。

---

### 2.3 程序运行界面

#### （1）购票登录页面

页面中央为登录表单，包含邮箱输入框和密码输入框，以及"登录并购票"按钮。支持邮箱格式登录（而非传统的用户名登录），登录成功后自动跳转到购票页面。登录失败时显示错误提示信息。

> 【此处插入购票登录页面截图】

#### （2）球队选择页面（购票入口）

以网格卡片形式展示中超16支球队，每张卡片包含球队队徽Logo和球队简称。用户点击某支球队即可进入该球队的购票页面。需登录后才能访问，未登录用户自动跳转到登录页。

> 【此处插入球队选择页面截图】

#### （3）SVG交互式选座页面

这是购票模块最核心的页面，分为以下几个区域：

- **比赛信息区**：展示下一场比赛的主客队信息和比赛时间
- **观赛人管理区**：AJAX动态管理观赛人列表（添加/编辑/删除），包含姓名、身份证号、手机号表单
- **SVG球场座位图**：以矢量图形绘制球场俯视图，分为4个颜色区域：
  - VIP区（红色，380元/张）
  - 主看台（蓝色，260元/张）
  - 侧看台（绿色，140元/张）
  - 球门后方（黄色，80元/张）
  
  用户点击不同区域选择座位（最多5张），已选座位高亮显示
- **浮动选座信息栏**：底部固定栏，实时显示已选座位数量和总价
- **支付弹窗**：点击支付后弹出模拟支付界面，显示支付宝扫码支付二维码和"已完成支付"确认按钮

> 【此处插入SVG选座页面截图】
> 【此处插入支付弹窗截图】

#### （4）支付成功页面

订单支付成功后跳转至此页面，显示订单号、比赛信息、购票数量、总金额、支付状态等信息。

> 【此处插入支付成功页面截图】

#### （5）Django Admin 订单管理界面

管理员可在后台查看所有购票订单，按订单状态（待支付/已支付/已取消）筛选，搜索订单号、用户名、身份证号等。订单号和创建时间设为只读字段。

> 【此处插入Admin后台订单管理截图】

---

### 2.4 关键代码说明

#### 代码文件清单

| 文件名 | 说明 |
|--------|------|
| `core/models.py`（TicketOrder、Viewer） | 订单和观赛人模型定义 |
| `core/backends.py` | 自定义邮箱认证后端 EmailAuthBackend |
| `core/views.py`（购票相关8个视图） | tickets、tickets_login、team_tickets、add_viewer、edit_viewer、delete_viewer、create_order、payment_success 等 |
| `core/admin.py`（TicketOrder、Viewer） | 订单/观赛人后台管理配置 |
| `core/urls.py`（购票相关路由） | 购票流程URL路由配置 |
| `templates/core/tickets.html` | 球队选择页模板 |
| `templates/core/tickets_login.html` | 购票登录页模板 |
| `templates/core/team_tickets.html` | SVG选座购票页模板（含大量JS交互代码） |
| `templates/core/payment_success.html` | 支付成功页模板 |
| `soccer_admin/settings.py`（支付宝配置部分） | 支付宝沙箱 APPID、密钥配置 |

---

#### （1）自定义邮箱认证后端 core/backends.py

```python
# core/backends.py

from django.contrib.auth.backends import ModelBackend
from django.contrib.auth import get_user_model

class EmailAuthBackend(ModelBackend):
    """支持使用邮箱登录"""

    def authenticate(self, request, username=None, password=None, **kwargs):
        User = get_user_model()
        if not username or not password:
            return None
        if "@" in username:
            try:
                user = User.objects.get(email=username)
            except User.DoesNotExist:
                return None
        else:
            try:
                user = User.objects.get(username=username)
            except User.DoesNotExist:
                return None
        if user.check_password(password):
            return user
        return None
```

该认证后端继承 Django 的 `ModelBackend`，重写 `authenticate` 方法。当传入的 `username` 参数包含 `@` 符号时，按邮箱字段查找用户；否则按用户名查找。需在 `settings.py` 中注册：

```python
AUTHENTICATION_BACKENDS = [
    "core.backends.EmailAuthBackend",
    "django.contrib.auth.backends.ModelBackend",
]
```

> 【此处插入 core/backends.py 代码拷屏】

---

#### （2）订单与观赛人模型 core/models.py

```python
# core/models.py

class TicketOrder(models.Model):
    """购票订单"""
    STATUS_CHOICES = [
        ("pending", "待支付"),
        ("paid", "已支付"),
        ("cancelled", "已取消"),
    ]

    user = models.ForeignKey(User, on_delete=models.CASCADE, verbose_name="用户")
    match = models.ForeignKey(Match, on_delete=models.CASCADE, verbose_name="比赛")
    ticket_price = models.PositiveIntegerField("票档价格")
    ticket_count = models.PositiveIntegerField("购票张数", default=1)
    id_card = models.CharField("身份证号", max_length=18)
    phone = models.CharField("手机号", max_length=11)
    total_amount = models.DecimalField("总金额", max_digits=10, decimal_places=2)
    order_number = models.CharField("订单号", max_length=32, unique=True)
    status = models.CharField("订单状态", max_length=20,
                              choices=STATUS_CHOICES, default="pending")
    payment_qr_code = models.TextField("支付二维码数据", blank=True)
    created_at = models.DateTimeField("创建时间", auto_now_add=True)
    paid_at = models.DateTimeField("支付时间", null=True, blank=True)

    class Meta:
        verbose_name = "购票订单"
        verbose_name_plural = "购票订单"
        ordering = ["-created_at"]


class Viewer(models.Model):
    """观影人"""
    user = models.ForeignKey(User, on_delete=models.CASCADE, verbose_name="用户")
    name = models.CharField("姓名", max_length=50)
    id_card = models.CharField("身份证号", max_length=18)
    phone = models.CharField("手机号", max_length=11)
    created_at = models.DateTimeField("创建时间", auto_now_add=True)

    class Meta:
        verbose_name = "观影人"
        verbose_name_plural = "观影人"
        ordering = ["-created_at"]
        unique_together = [["user", "id_card"]]
```

> 【此处插入 core/models.py 订单和观赛人模型代码拷屏】

---

#### （3）购票登录视图 core/views.py（tickets_login函数）

```python
# core/views.py

def tickets_login(request):
    """购票登录：邮箱+密码"""
    if request.user.is_authenticated:
        return redirect("core:tickets")
    if request.method == "POST":
        email = request.POST.get("email", "").strip()
        password = request.POST.get("password", "")
        if not email or not password:
            messages.error(request, "请输入邮箱和密码")
            return render(request, "core/tickets_login.html")
        user = authenticate(request, username=email, password=password)
        if user is not None:
            login(request, user)
            next_url = request.GET.get("next") or "core:tickets"
            return redirect(next_url)
        messages.error(request, "邮箱或密码错误")
    return render(request, "core/tickets_login.html")
```

> 【此处插入 tickets_login 视图代码拷屏】

---

#### （4）AJAX添加观赛人 core/views.py（add_viewer函数）

```python
# core/views.py

@login_required(login_url="/tickets/login/")
@require_http_methods(["POST"])
def add_viewer(request):
    """添加观影人"""
    try:
        name = request.POST.get("name", "").strip()
        id_card = request.POST.get("id_card", "").strip()
        phone = request.POST.get("phone", "").strip()
        if not name or not id_card or not phone:
            return JsonResponse({"success": False, "message": "请填写完整信息"})
        if Viewer.objects.filter(user=request.user, id_card=id_card).exists():
            return JsonResponse({"success": False, "message": "该身份证号已存在"})
        viewer = Viewer.objects.create(
            user=request.user, name=name, id_card=id_card, phone=phone)
        return JsonResponse({
            "success": True,
            "viewer": {"id": viewer.id, "name": viewer.name,
                       "id_card": viewer.id_card, "phone": viewer.phone},
        })
    except Exception as e:
        return JsonResponse({"success": False, "message": str(e)})
```

该视图通过 AJAX POST 请求接收观赛人信息，验证数据完整性和身份证号唯一性后创建记录，返回 JSON 响应。前端 JavaScript 根据响应动态更新观赛人列表，无需刷新页面。

> 【此处插入 add_viewer 视图代码拷屏】

---

#### （5）创建订单并对接支付宝 core/views.py（create_order函数）

```python
# core/views.py

@login_required(login_url="/tickets/login/")
@require_http_methods(["POST"])
def create_order(request):
    """创建购票订单 → 返回支付宝支付链接"""
    from alipay import AliPay
    from django.conf import settings as conf

    try:
        match_id = request.POST.get("match_id")
        ticket_price = int(request.POST.get("ticket_price"))
        ticket_count = int(request.POST.get("ticket_count", 1))
        viewer_id = request.POST.get("viewer_id", "").strip()
        id_card = request.POST.get("id_card", "").strip()
        phone = request.POST.get("phone", "").strip()

        if viewer_id:
            viewer = get_object_or_404(Viewer, pk=viewer_id, user=request.user)
            id_card, phone = viewer.id_card, viewer.phone

        match = get_object_or_404(Match, pk=match_id)
        total_amount = Decimal(str(ticket_price * ticket_count))
        order_number = f"T{timezone.now().strftime('%Y%m%d%H%M%S')}"
                       f"{uuid.uuid4().hex[:8].upper()}"

        order = TicketOrder.objects.create(
            user=request.user, match=match,
            ticket_price=ticket_price, ticket_count=ticket_count,
            id_card=id_card, phone=phone,
            total_amount=total_amount, order_number=order_number,
        )

        # 构建支付宝支付链接
        alipay_client = AliPay(
            appid=conf.ALIPAY_APPID,
            app_notify_url=None,
            app_private_key_string=conf.ALIPAY_APP_PRIVATE_KEY,
            alipay_public_key_string=conf.ALIPAY_PUBLIC_KEY,
            sign_type="RSA2", debug=True,
        )

        subject = f"中超球票 - {match.home_team.short_name} vs "
                  f"{match.away_team.short_name}"
        base_url = request.build_absolute_uri("/")
        order_string = alipay_client.api_alipay_trade_page_pay(
            out_trade_no=order_number,
            total_amount=str(total_amount),
            subject=subject,
            return_url=base_url + "tickets/alipay/return/",
            notify_url=base_url + "tickets/alipay/notify/",
        )
        pay_url = alipay_client._gateway + "?" + order_string

        return JsonResponse({
            "success": True,
            "order_number": order_number,
            "total_amount": str(total_amount),
            "pay_url": pay_url,
        })
    except Exception as e:
        return JsonResponse({"success": False,
                            "message": f"创建订单失败：{str(e)}"})
```

该视图是购票流程的核心，完成以下步骤：
1. 从请求中获取比赛ID、票价、数量、观赛人信息
2. 生成唯一订单号（时间戳+UUID）
3. 创建 TicketOrder 数据库记录
4. 使用 python-alipay-sdk 构建支付宝支付链接
5. 返回 JSON 响应，前端跳转到支付宝支付页面

> 【此处插入 create_order 视图代码拷屏】

---

#### （6）购票相关URL路由 core/urls.py

```python
# core/urls.py（购票相关路由部分）

urlpatterns = [
    # ... 其他路由
    path("tickets/", views.tickets, name="tickets"),
    path("tickets/login/", views.tickets_login, name="tickets_login"),
    path("tickets/logout/", views.tickets_logout, name="tickets_logout"),
    path("tickets/team/<int:team_id>/", views.team_tickets, name="team_tickets"),
    path("tickets/viewers/add/", views.add_viewer, name="add_viewer"),
    path("tickets/viewers/<int:viewer_id>/edit/", views.edit_viewer, name="edit_viewer"),
    path("tickets/viewers/<int:viewer_id>/delete/", views.delete_viewer, name="delete_viewer"),
    path("tickets/order/create/", views.create_order, name="create_order"),
    path("tickets/pay/<str:order_number>/", views.payment_qr, name="payment_qr"),
    path("tickets/pay/<str:order_number>/confirm/", views.confirm_payment, name="confirm_payment"),
    path("tickets/alipay/return/", views.alipay_return, name="alipay_return"),
    path("tickets/alipay/notify/", views.alipay_notify, name="alipay_notify"),
    path("tickets/success/<str:order_number>/", views.payment_success, name="payment_success"),
]
```

购票模块共配置了13条URL路由，覆盖了从登录、选球队、管理观赛人、创建订单到支付回调的完整流程。

> 【此处插入 core/urls.py 购票路由部分代码拷屏】

---

#### （7）订单后台管理 core/admin.py

```python
# core/admin.py

@admin.register(TicketOrder)
class TicketOrderAdmin(admin.ModelAdmin):
    list_display = ["order_number", "user", "match", "ticket_price",
                    "ticket_count", "total_amount", "status", "created_at"]
    list_filter = ["status", "created_at"]
    search_fields = ["order_number", "user__username", "id_card", "phone"]
    readonly_fields = ["order_number", "created_at", "paid_at"]

@admin.register(Viewer)
class ViewerAdmin(admin.ModelAdmin):
    list_display = ["name", "id_card", "phone", "user", "created_at"]
    list_filter = ["user"]
    search_fields = ["name", "id_card", "phone"]
```

> 【此处插入 core/admin.py 订单管理代码拷屏】

---

#### （8）支付宝沙箱配置 soccer_admin/settings.py

```python
# soccer_admin/settings.py

# ── 支付宝沙箱环境配置 ──
ALIPAY_APPID = "9021000162678789"
ALIPAY_GATEWAY = "https://openapi-sandbox.dl.alipaydev.com/gateway.do"
ALIPAY_APP_PRIVATE_KEY = """-----BEGIN RSA PRIVATE KEY-----
MIIEvwIBADANBgkqhkiG9w0BAQEFA...（RSA2 私钥）
-----END RSA PRIVATE KEY-----"""
ALIPAY_PUBLIC_KEY = """-----BEGIN PUBLIC KEY-----
MIIBIjANBgkqhkiG9w0BAQEFAAOCA...（支付宝公钥）
-----END PUBLIC KEY-----"""
```

使用支付宝沙箱环境进行开发测试，配置了应用 APPID、网关地址、应用私钥和支付宝公钥，采用 RSA2 签名方式确保支付数据安全。

> 【此处插入 settings.py 支付宝配置部分代码拷屏】
