# 项目1：中超联赛赛事管理系统

## 1 项目说明

### 项目功能

本项目是一个中超联赛赛事管理系统，实现了以下核心功能：
- **球队管理**：管理中超16支球队的基本信息（名称、城市、主场、队徽等）
- **球员管理**：管理各球队球员信息及统计数据（进球、助攻、红黄牌、身价等）
- **新闻展示**：发布和展示中超联赛相关新闻资讯
- **联赛积分榜**：展示各赛季球队积分排名与多维统计数据
- **赛程查询**：查询历年中超联赛赛程安排
- **在线购票**：用户登录后可选座购票，集成支付宝沙箱支付

### 所用技术

| 类别 | 技术 |
|------|------|
| 后端框架 | Python 3 + Django 6.0.2 |
| 数据库 | SQLite3 |
| 前端 | HTML5 + CSS3 + JavaScript + Bootstrap |
| 支付集成 | 支付宝沙箱环境（python-alipay-sdk） |
| 版本控制 | Git |

### 运行平台

- 操作系统：macOS / Windows / Linux
- Python版本：3.10+
- 浏览器访问：http://127.0.0.1:8000/

---

## 2 自己工作说明

### 2.1 工作任务概述

我负责系统的**联赛数据统计模块**和**赛程管理模块**的开发。具体工作包括：

1. 设计并实现 `TeamStanding`（球队赛季统计）数据模型，支持1994-2026年多年份历史数据
2. 设计并实现 `Match`（比赛）数据模型，包含主客队、比赛时间、轮次、场地等信息
3. 开发球队统计榜视图，支持按积分、进球、助攻、犯规等13个维度排序展示
4. 开发球员排行榜视图，展示Top 100球员（按进球、助攻、身价等16个维度）
5. 开发赛程查询视图，支持按年份筛选
6. 设计前端统计页面，使用JavaScript实现侧边栏无刷新切换统计维度
7. 配置 Django Admin 后台，支持 `list_editable` 快速编辑联赛数据
8. 编写数据迁移脚本，导入2020-2026年联赛积分数据和赛程数据

---

### 2.2 数据库表结构

#### （1）leagues_teamstanding 表（球队赛季统计表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 主键，自增 |
| team_id | INTEGER | 外键，关联 teams_team |
| year | INTEGER | 赛季年份（1994-2026） |
| points | INTEGER | 积分 |
| matches_played | INTEGER | 比赛场次 |
| wins | INTEGER | 胜场 |
| draws | INTEGER | 平场 |
| losses | INTEGER | 负场 |
| goals_for | INTEGER | 进球数 |
| goals_against | INTEGER | 失球数 |
| assists | INTEGER | 助攻数 |
| yellow_cards | INTEGER | 黄牌数 |
| red_cards | INTEGER | 红牌数 |
| penalties | INTEGER | 点球数 |
| shots | INTEGER | 射门数 |
| shots_on_target | INTEGER | 射正数 |
| offsides | INTEGER | 越位数 |
| corners | INTEGER | 角球数 |
| passes | INTEGER | 传球数 |
| fouls | INTEGER | 犯规数 |
| saves | INTEGER | 扑救数 |

**约束**：`(team_id, year)` 联合唯一；默认按 `-points, -goals_for, goals_against` 排序。

#### （2）matches_match 表（比赛表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 主键，自增 |
| home_team_id | INTEGER | 外键，主队（关联 teams_team） |
| away_team_id | INTEGER | 外键，客队（关联 teams_team） |
| match_time | DATETIME | 比赛时间 |
| round_number | INTEGER | 比赛轮次 |
| stadium | VARCHAR(100) | 比赛场地 |
| tickets_open | BOOLEAN | 是否已开票 |
| created_at | DATETIME | 创建时间 |

---

### 2.3 程序运行界面

#### （1）球队统计榜页面

页面左侧为统计维度侧边栏菜单，包含：积分榜、进球榜、失球榜、助攻榜、黄牌榜、红牌榜、点球榜、射门榜、射正榜、越位榜、角球榜、传球榜、犯规榜、扑救榜共14个维度。右侧为数据表格，展示各球队在所选维度下的排名数据。顶部有年份下拉选择器（1994-2026），切换年份可查看历史赛季数据。点击左侧不同维度菜单项，页面通过JavaScript无刷新切换表格内容。

> 【此处插入球队统计榜页面截图】

#### （2）球员排行榜页面

与球队统计榜类似的布局，左侧为统计维度菜单（进球榜、助攻榜、黄牌榜、红牌榜、点球榜、射门榜、射正榜、过人榜、犯规榜、被犯规榜、传球榜、传中榜、抢断榜、解围榜、扑救榜、身价榜共16个维度），右侧展示 Top 100 球员排名表格，包含排名、球员姓名、所属球队、统计数值等列。过滤掉统计值为0的球员。

> 【此处插入球员排行榜页面截图】

#### （3）赛程表页面

页面顶部为年份下拉选择器，主体为赛程列表，按比赛时间顺序排列。每场比赛显示：轮次、主队Logo和队名、"vs"、客队Logo和队名、比赛时间、比赛场地。支持切换不同年份查看历史赛程。

> 【此处插入赛程表页面截图】

#### （4）Django Admin 联赛数据管理界面

后台管理界面使用 `list_editable` 配置，允许管理员在列表页面直接编辑球队的各项统计数据（积分、胜/平/负、进球、失球等），无需进入详情页即可快速修改，极大提高了数据录入效率。

> 【此处插入Admin后台联赛数据管理截图】

---

### 2.4 关键代码说明

#### 代码文件清单

| 文件名 | 说明 |
|--------|------|
| `leagues/models.py` | TeamStanding 模型定义 |
| `leagues/admin.py` | 联赛数据后台管理配置 |
| `leagues/urls.py` | 联赛URL路由 |
| `matches/models.py` | Match 模型定义 |
| `matches/admin.py` | 赛程后台管理配置 |
| `matches/urls.py` | 赛程URL路由 |
| `core/views.py`（team_standings函数） | 球队统计榜视图 |
| `core/views.py`（player_rankings函数） | 球员排行榜视图 |
| `core/views.py`（schedule函数） | 赛程查询视图 |
| `core/urls.py`（standings/players/schedule路由） | 对应URL路由配置 |
| `templates/core/team_standings.html` | 球队统计榜模板 |
| `templates/core/player_rankings.html` | 球员排行榜模板 |
| `templates/core/schedule.html` | 赛程表模板 |

---

#### （1）球队赛季统计模型 leagues/models.py

```python
# leagues/models.py

from django.db import models
from teams.models import Team

class TeamStanding(models.Model):
    """积分榜"""
    team = models.ForeignKey(Team, on_delete=models.CASCADE,
                             related_name="standings", verbose_name="球队")
    year = models.PositiveIntegerField("年份", default=2026)
    points = models.PositiveIntegerField("胜点", default=0)
    matches_played = models.PositiveIntegerField("场次", default=0)
    wins = models.PositiveIntegerField("胜", default=0)
    draws = models.PositiveIntegerField("平", default=0)
    losses = models.PositiveIntegerField("负", default=0)
    goals_for = models.PositiveIntegerField("进球", default=0)
    goals_against = models.PositiveIntegerField("失球", default=0)
    assists = models.PositiveIntegerField("助攻", default=0)
    yellow_cards = models.PositiveIntegerField("黄牌", default=0)
    red_cards = models.PositiveIntegerField("红牌", default=0)
    penalties = models.PositiveIntegerField("点球", default=0)
    shots = models.PositiveIntegerField("射门", default=0)
    shots_on_target = models.PositiveIntegerField("射正", default=0)
    offsides = models.PositiveIntegerField("越位", default=0)
    corners = models.PositiveIntegerField("角球", default=0)
    passes = models.PositiveIntegerField("传球", default=0)
    fouls = models.PositiveIntegerField("犯规", default=0)
    saves = models.PositiveIntegerField("扑救", default=0)

    class Meta:
        verbose_name = "积分"
        verbose_name_plural = "积分榜"
        ordering = ["-points", "-goals_for", "goals_against"]
        unique_together = [["team", "year"]]

    def __str__(self):
        return f"{self.team.short_name} - {self.points}分"

    @property
    def goal_difference(self):
        """净胜球"""
        return self.goals_for - self.goals_against
```

> 【此处插入 leagues/models.py 代码拷屏】

---

#### （2）比赛模型 matches/models.py

```python
# matches/models.py

from datetime import timedelta
from django.db import models
from django.utils import timezone
from teams.models import Team

class Match(models.Model):
    """比赛"""
    home_team = models.ForeignKey(Team, on_delete=models.CASCADE,
                                  related_name="home_matches", verbose_name="主队")
    away_team = models.ForeignKey(Team, on_delete=models.CASCADE,
                                  related_name="away_matches", verbose_name="客队")
    match_time = models.DateTimeField("比赛时间")
    round_number = models.PositiveIntegerField("轮次")
    stadium = models.CharField("场地", max_length=100, blank=True)
    tickets_open = models.BooleanField("已开票", default=False)
    created_at = models.DateTimeField("创建时间", auto_now_add=True)

    @property
    def is_tickets_open(self):
        """比赛前 7 天自动开票"""
        now = timezone.now()
        return now >= self.match_time - timedelta(days=7) and now < self.match_time

    class Meta:
        verbose_name = "比赛"
        verbose_name_plural = "比赛"
        ordering = ["match_time"]

    def __str__(self):
        return f"{self.home_team.short_name} vs {self.away_team.short_name}"
```

`is_tickets_open` 是一个计算属性，根据当前时间自动判断比赛是否已进入开票时间窗口（比赛前7天至比赛开始）。

> 【此处插入 matches/models.py 代码拷屏】

---

#### （3）球队统计榜视图 core/views.py（team_standings函数）

```python
# core/views.py

def team_standings(request):
    """球队榜"""
    try:
        year = int(request.GET.get('year', 2026))
        if year < 1994 or year > 2026:
            year = 2026
    except (ValueError, TypeError):
        year = 2026
    years = list(range(1994, 2027))

    standings = TeamStanding.objects.select_related("team").filter(
        year=year
    ).order_by("-points", "-goals_for", "goals_against")

    # 为各个统计项创建排序后的列表
    standings_list = list(standings)
    standings_by_goals_for = sorted(standings_list,
                                     key=lambda x: x.goals_for, reverse=True)
    standings_by_goals_against = sorted(standings_list,
                                        key=lambda x: x.goals_against)
    standings_by_assists = sorted(standings_list,
                                   key=lambda x: x.assists, reverse=True)
    standings_by_yellow_cards = sorted(standings_list,
                                       key=lambda x: x.yellow_cards, reverse=True)
    # ... 其他维度类似
    standings_by_saves = sorted(standings_list,
                                 key=lambda x: x.saves, reverse=True)

    context = {
        "standings": standings,
        "standings_by_goals_for": standings_by_goals_for,
        "standings_by_goals_against": standings_by_goals_against,
        "standings_by_assists": standings_by_assists,
        # ... 其他维度
        "years": years,
        "current_year": year,
    }
    return render(request, "core/team_standings.html", context)
```

该视图从数据库中查询指定年份的球队统计数据，然后在Python端为13个不同统计维度分别排序，将排序结果传递给模板。前端通过JavaScript侧边栏菜单切换不同维度表格的显示/隐藏，实现无刷新切换效果。

> 【此处插入 team_standings 视图代码拷屏】

---

#### （4）球员排行榜视图 core/views.py（player_rankings函数）

```python
# core/views.py

def player_rankings(request):
    """球员榜"""
    try:
        year = int(request.GET.get('year', 2026))
        if year < 1994 or year > 2026:
            year = 2026
    except (ValueError, TypeError):
        year = 2026
    years = list(range(1994, 2027))

    players = Player.objects.select_related("team").filter(year=year).all()
    players_list = list(players)

    def get_sorted_players(key_func, filter_func):
        """辅助函数：排序并过滤球员列表"""
        return [p for p in sorted(players_list, key=key_func, reverse=True)
                if filter_func(p)][:100]

    players_by_goals = get_sorted_players(
        lambda p: p.goals, lambda p: p.goals > 0)
    players_by_assists = get_sorted_players(
        lambda p: p.assists, lambda p: p.assists > 0)
    players_by_market_value = get_sorted_players(
        lambda p: float(p.market_value), lambda p: float(p.market_value) > 0)
    # ... 其他维度类似

    context = {
        "players_by_goals": players_by_goals,
        "players_by_assists": players_by_assists,
        "players_by_market_value": players_by_market_value,
        # ... 其他维度
        "years": years,
        "current_year": year,
    }
    return render(request, "core/player_rankings.html", context)
```

该视图使用 `get_sorted_players` 辅助函数，对球员列表按不同统计维度排序并过滤掉统计值为0的球员，只取前100名，避免展示无意义的数据。

> 【此处插入 player_rankings 视图代码拷屏】

---

#### （5）赛程查询视图 core/views.py（schedule函数）

```python
# core/views.py

def schedule(request):
    """赛程页：按时间顺序展示中超全部比赛"""
    try:
        year = int(request.GET.get('year', 2026))
        if year < 1994 or year > 2026:
            year = 2026
    except (ValueError, TypeError):
        year = 2026
    years = list(range(1994, 2027))

    matches = Match.objects.select_related("home_team", "away_team").filter(
        match_time__year=year
    ).order_by("match_time")

    context = {
        "matches": matches,
        "years": years,
        "current_year": year,
    }
    return render(request, "core/schedule.html", context)
```

使用 Django ORM 的 `match_time__year` 查询条件按年份过滤比赛记录，并通过 `select_related` 预加载主客队信息避免 N+1 查询问题。

> 【此处插入 schedule 视图代码拷屏】

---

#### （6）联赛数据后台管理 leagues/admin.py

```python
# leagues/admin.py

from django.contrib import admin
from .models import TeamStanding

@admin.register(TeamStanding)
class TeamStandingAdmin(admin.ModelAdmin):
    list_display = [
        "team", "year", "points", "matches_played", "wins", "draws",
        "losses", "goals_for", "goals_against", "goal_difference_display",
        "assists", "yellow_cards", "red_cards", "penalties", "shots",
        "shots_on_target", "offsides", "corners", "passes", "fouls", "saves",
    ]
    list_editable = [
        "points", "matches_played", "wins", "draws", "losses",
        "goals_for", "goals_against", "assists", "yellow_cards",
        "red_cards", "penalties", "shots", "shots_on_target",
        "offsides", "corners", "passes", "fouls", "saves",
    ]
    list_filter = ["year", "team"]
```

通过 `list_editable` 配置，管理员可以在列表页面直接编辑所有统计字段，无需进入编辑页面，大幅提高数据维护效率。

> 【此处插入 leagues/admin.py 代码拷屏】
